{
  "name": "channel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vp run task:vanilla:build",
    "build:vanilla-workers": "vp run task:vanilla-workers:build",
    "changeset": "changeset",
    "check": "vp run task:workspace:check",
    "dev:package": "vp run task:channel:dev",
    "dev:vanilla": "vp run task:vanilla:dev",
    "dev:vanilla-workers": "vp run task:vanilla-workers:dev",
    "fmt": "vp run task:workspace:fmt",
    "lint": "vp run task:workspace:lint",
    "pack": "vp run task:channel:pack",
    "ready": "vp run task:ready",
    "release": "vp run task:ready && changeset publish",
    "test": "vp run task:channel:test",
    "version": "changeset version"
  },
  "devDependencies": {
    "@changesets/changelog-github": "catalog:",
    "@changesets/cli": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.5.2"
}
