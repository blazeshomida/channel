{
  "name": "channel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "vp check",
    "test": "vp run -r test",
    "build": "vp run -r build",
    "pack": "vp run @blazeshomida/channel#pack",
    "dev:package": "vp run @blazeshomida/channel#dev",
    "ready": "vp check && vp run -r test && vp run -r build"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.5.2"
}
